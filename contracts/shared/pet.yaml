components:
  schemas:

    Pet:
      type: object
      required:
        - id
        - name
        - age
        - type
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        age:
          type: integer
        type:
          type: string